/**
 * Daily blog mill — disabled.
 *
 * Google Search: generating many pages with generative AI without adding value
 * may violate scaled content abuse.
 * https://developers.google.com/search/docs/fundamentals/using-gen-ai-content
 *
 * New posts: Master SEO Agent Rule G → blog-module.ts
 * Manual: smart-blog-agent.ts (aborts unless Search Console shows a real gap)
 */

async function main() {
  console.log("⛔ Daily blog mill is disabled.");
  console.log("   Scaled AI publishing without original value is spam policy (QRG 4.6.5 / 4.6.6).");
  console.log("   Use the Master SEO Agent Rule G or smart-blog-agent.ts when there is a grounded keyword gap.");
}

main();
